<div class='container'>
  <% if @supplement.present? %>
    <h1><%= @supplement.name %></h1>
    <p class='fs-4'><%= @supplement.with_food_instruction %></p>
  <% end %>
  <%= link_to 'Back to list', supplements_path, class: 'btn btn-primary fs-5' %>
</div>
